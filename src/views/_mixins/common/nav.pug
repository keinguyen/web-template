mixin nav(items = [])
  ul
    each item in items
      li
        +link(`/${$localeName + item.href}`, item.title, item.text)(class=item.class)
        if item.itemsMenu && item.itemsMenu[0]
          +nav(item.itemsMenu)
